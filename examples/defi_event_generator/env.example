# Required
DELTASTREAM_TOKEN=your-api-token-here
DELTASTREAM_ORG_ID=your-organization-id-here

# Optional
DELTASTREAM_SERVER_URL=https://api.deltastream.io/v2
DELTASTREAM_DATABASE_NAME=default

# Store Configuration
STORE_NAME=kafka_store

# Event Generation Configuration
# Interval in seconds between normal traffic generation
NORMAL_TRAFFIC_INTERVAL=5

# Interval in seconds between flash loan attack simulations
ATTACK_INTERVAL=120

# Total duration in seconds (0 = run indefinitely)
DURATION=0
